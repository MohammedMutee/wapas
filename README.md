# Wapas — revenue that came back

**Razorpay AI Buildathon · Track 03 · AI Revenue Recovery**

An agent that detects revenue at risk, diagnoses why it is at risk, executes a
bounded recovery workflow under deterministic guardrails, and **proves in rupees
what it recovered against a control group it never touched.**

> **Status: in development.** The headline table below is generated by CI from a
> seeded evaluation run. Rows marked `—` are not built yet. Nothing in this
> README is hand-typed; if a number is here, `make eval` reproduces it.

---

## Results

_Generated by `make eval-llm` · seed `20260901` · episodes `5,000` · treatment arm = the LLM agent_

| Metric | Value |
|---|---|
| Total revenue at risk | ₹1,16,43,570 |
| Gross recovered (treatment) | ₹22,84,424 |
| Randomised control, no treatment (scaled) | ₹5,36,526 |
| **Net incremental recovery vs control** | **₹17,47,443** (95% CI [₹14,44,540, ₹20,71,758], p = 0.0001) |
| Modelled cost of opt-outs, complaints, disputes | less ₹2,75,622 → **₹14,71,820** |
| **Diagnosis accuracy** | **88.6%**, against an oracle ceiling of 90.5% |
| Forbidden retries per 1,000 episodes | **0.0** — the industry-default fixed ladder runs 965 |
| Policy violations reaching an actuator | **0** |
| Red-team escapes | **0 of 33** ([`results/redteam.md`](results/redteam.md)) |
| Beats the fixed ladder on rupees? | **no** — behind, p = 0.10 |

### The one number that matters, and it is not the average

Diagnosis is three different problems, so it is measured as three. Both the
agent and the keyword baseline are given the merchant's **resolved history** —
denying a baseline that would be a strawman, because for a fixed vocabulary of
error strings a lookup is *optimal*.

| | model | keyword classifier | ceiling |
|---|---|---|---|
| Wording seen in history (65%) | 100% | 100% | 100% |
| **Wording never seen before (17%)** | **96.8%** | **66.4%** | 100% |
| Text identifies nothing (18%) | 39.2% | 41.2% | 44.5% |
| **Overall** | **88.6%** | 83.2% | 90.5% |

A lookup table is unbeatable on text it has seen and falls off a cliff the
first time an acquirer rewords a decline. That cliff is the entire case for
putting a model in this system — one column wide, and worth 5.4 points overall.

The keyword table is **frozen** against those unseen wordings: no keyword may
ever be added to accommodate them, and the git history makes that checkable.
Without that rule the column measures the author's foresight, not the method.

Full report: [`results/report.md`](results/report.md) · Statistical calibration:
[`results/calibration.md`](results/calibration.md) · Architecture:
[`ARCHITECTURE.md`](ARCHITECTURE.md) · Decision log: [`DECISIONS.md`](DECISIONS.md)

---

## Why a control group

A large share of failed payments recover on their own — the customer simply
tries again. Any system reporting *gross* recovery is claiming credit for
customers it never touched.

Wapas randomly assigns 15% of episodes to a **control arm that receives no
treatment at all**, and reports the difference. The headline metric is
**net incremental recovery**: what the agent recovered beyond self-recovery,
minus every rupee it spent on channels and tokens to get there.

Assignment is stratified by amount decile, the decision rule is a stratified
permutation test, and the report publishes a **placebo comparison** — one arm
split in two — beside every real claim, so a reader can see what the harness
reports when there is nothing to report. `make calibrate` measures the
false-positive rate over hundreds of known nulls rather than assuming it.

---

## Run it

```bash
make install     # venv + dependencies
make up          # Postgres + Redis via docker compose
make migrate     # schema
make test        # unit + property tests
make warm        # pre-fetch every distinct diagnosis prompt into .cache/
make eval        # seeded batch, rules planner → results/report.md
make eval-llm    # same, with the LLM agent as the treatment arm
make calibrate   # false-positive rate on known nulls → results/calibration.md
make sweep       # ±30% on every parameter → results/sensitivity.md
make redteam     # 33 adversarial scenarios; expect 0 escapes
```

## What exists today

| Component | State | Notes |
|---|---|---|
| Money handling (integer paise, INR formatting) | ✅ | `src/wapas/money.py` |
| Virtual clock + deterministic scheduler | ✅ | 90 simulated days in seconds |
| Domain model, root-cause taxonomy, state machine table | ✅ | `src/wapas/domain.py` |
| Hash-chained audit log + verification | ✅ | tamper, reorder, deletion and back-dating all detected |
| Policy gate (contact, money, escalation) | ✅ | property-tested |
| Provider-neutral LLM layer (NVIDIA NIM) | ✅ | capability ladder + validate-and-retry |
| Model bake-off | ✅ | [`results/model_bakeoff.md`](results/model_bakeoff.md) |
| Database schema + migrations | ✅ | 8 tables; audit table append-only via DB trigger |
| Right-to-erasure (redaction, not deletion) | ✅ | `src/wapas/db/erasure.py` |
| Episode engine + state machine | ✅ | 8 terminal states, hard step bound |
| Strategies: control, naive, blast, rules-only | ✅ | `src/wapas/strategies/` |
| Simulator + published parameters | ✅ | `sim/params.yaml`, 5,000 seeded episodes |
| Batch evaluation + report | ✅ | [`results/report.md`](results/report.md), CI-reproducible |
| Stratified allocation + permutation test + placebo | ✅ | `eval/stats.py`, `wapas.engine.stratified_assignment` |
| Self-calibration on known nulls | ✅ | [`results/calibration.md`](results/calibration.md) |
| Externality pricing (opt-out, complaint, dispute) | ✅ | `config/rates.yaml`, reported separately |
| LLM diagnosis (structured output, cached, priced) | ✅ | `src/wapas/diagnose/`, NVIDIA NIM |
| Calibration validator on model output | ✅ | a generic failure signal cannot support a confident answer |
| Actuators against real Razorpay APIs | ⬜ | next |
| Sensitivity sweep (±30% on every parameter) | ✅ | [`results/sensitivity.md`](results/sensitivity.md) |
| Adversarial suite | ✅ | [`results/redteam.md`](results/redteam.md), 33 scenarios |
| CI regenerates the report and checks the README | ✅ | `.github/workflows/ci.yml` |
| Dashboard | ⬜ | |

## Architecture in one line

**The LLM proposes; deterministic code disposes.** A language model can be
persuaded; a policy engine cannot. Every action passes schema validation and
then a policy gate written in plain Python with no prompt and no context
window. That separation is why the red-team result can be a number rather than
an impression.

See [`ARCHITECTURE.md`](ARCHITECTURE.md).

## Honest limitations

- **Metrics are simulator-produced.** The generative parameters live in
  `sim/params.yaml` and are published. The agent never sees them. A sensitivity
  sweep perturbs every parameter ±30% to show the result is not parameter-tuned.
- **Compliance rules are an engineering model, not legal advice.** Every rule in
  `policies/*.yaml` carries `source` and `verified`. Rules with
  `verified: false` have not been independently confirmed and are not presented
  as regulatory citations. See [`docs/compliance.md`](docs/compliance.md).
- **Test mode only.** No live-mode code path exists in this repository. The
  configuration loader refuses to start with a key beginning `rzp_live_`.
- **Checkout abandonment is deliberately out of scope.** Reasoning in
  [`docs/scope.md`](docs/scope.md).

## Licence

MIT.
