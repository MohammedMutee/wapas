.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.env
*.db
node_modules/
web/.next/
results/*.png
!results/.gitkeep
.DS_Store

# Credentials — never commit. .env is the only place keys live.
*.csv
*-key*
*key*.txt
*secret*
*credential*
.secrets/

# Local model-response cache. Deliberately not committed: shipping cached
# results without the calls that produced them is shipping a conclusion,
# not evidence. "make warm" rebuilds it.
.cache/

# Local render checks (playwright screenshots, not committed)
*.png
